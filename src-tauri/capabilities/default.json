{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the PCPanel main window. The UI is served by the local backend and does not call Tauri APIs itself — the shell drives everything from Rust — so only the core defaults are granted.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
